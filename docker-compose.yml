version: '3.8'

services:
  registry:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-ai-agents-registry
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
